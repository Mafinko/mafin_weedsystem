# ox_inventory

Copy the two entries from `items.lua` into:

```text
ox_inventory/data/items.lua
```

Copy `images/cannabis.png` and `images/marijuana.png` into:

```text
ox_inventory/web/images
```

Then set this in `mafin_weedsystem/config.lua`:

```lua
Config.AutoRegisterItems = false
```

